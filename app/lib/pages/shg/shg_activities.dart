import 'package:flutter/material.dart';

class SHGActivities extends StatelessWidget {
  const SHGActivities({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
