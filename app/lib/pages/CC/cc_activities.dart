import 'package:app/constants/env.dart';
import 'package:flutter/material.dart';

class CCActivities extends StatelessWidget {
  const CCActivities({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [

        ],
      )
    );
  }
}
